extends main

block meta
    title Articles on my site!
    meta(name='description', content='A list of articles!')

block content
    h1 Articles
    - import std.algorithm;
    - foreach(name, content; contents)
        a(href=name)
            h2 #{ content["title"] }
        time #{ content["date"] }
        p !{ content["abstract"] }







