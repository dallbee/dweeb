extends main

block meta
    title UPDATE
    meta(name='description', content='UPDATE')

block content
    h1 UPDATE
    - import std.algorithm;
    - foreach(name, content; contents)
        section
            a(href=name)
                h2 #{ content["title"] }
            p !{ content["abstract"] }
            time #{ content["date"] }







