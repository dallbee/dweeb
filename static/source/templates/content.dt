extends main

block meta
    title #{view.page["title"]}
    meta(name='description', content='#{view.page["description"]}')

block content
    h2 #{view.page["title"]}
    -import helper.view;
    !{view.page["content"].parseMarkdown}






